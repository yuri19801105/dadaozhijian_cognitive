# 本地开发用（git-crypt 加密，不提交明文）
# 生产环境通过 git-crypt unlock 或 External Secrets Operator 注入
[llm]
api_key = "sk-local-dev-placeholder"
base_url = "https://api.openai.com/v1"
model = "gpt-4o-mini"

[prometheus]
pushgateway_url = ""
job_name = "dadaozhijian-dev"

[k8s]
kubeconfig = ""
namespace = "dadaozhijian-dev"