# 复制为 config/secrets.toml 并填入真实值，再 git-crypt lock
# 此文件不提交明文；CI 中通过 git-crypt 解密或 External Secrets Operator 注入
[llm]
api_key = ""           # OpenAI 兼容 API Key
base_url = "https://api.openai.com/v1"
model = "gpt-4o-mini"

[prometheus]
pushgateway_url = ""   # 可选：Pushgateway 地址
job_name = "dadaozhijian"

[k8s]
kubeconfig = ""        # base64 编码的 kubeconfig，或留空使用 in-cluster config
namespace = "dadaozhijian"